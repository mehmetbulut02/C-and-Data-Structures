#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    /*
     int i;
     for(i=0;i<5;i++){
     printf("2 uzeri %d : %.f\n",i,exp2(i));         // 2 uzeri sayi
     }
    /*
    int i;
    for(i=1;i<20;i++){
        printf("karekok %d : %.2f\n",i,sqrt(i)); //  not: %.2f deki 2 sonucta kac basamak gormek istedigimizi gosterir
    }

    /*
    printf("log(3): %.2f\n",log(3));

    /*
    int number1,number2;
    printf("Enter two numbers:");
    scanf("%d%d",&number1,&number2);
    printf("%d uzeri %d: %.2f\n",number1,number2,pow(number1,number2));



    double x=216,result;
    result=cbrt(x);
    printf("Result:%2f\n",result);      // kup kok

    // diger matematk fonksiyonlari

    /*
    ceil : girilen ondalik sayiyi tavana yuvarlar 6.2 -> 7
    floor : girilen ondalikli sayiyi zemine yuvarlar 6.8 -> 6
    fabs : ondalikli sayinin mutlak degerini verir
    abs : tam sayinin mutlak degerini verir
    fdim : iki ondalikli sayinin farki pozitifse sonucu negatifse 0'i dondurur
    fma : kendisine gonderilen ilk 2 sayiyi carpip ucuncusu ile toplar -> (x*y)+z
    fmax : iki float degerden max olani yazdirir
    fmin : iki float degerden öin olani yazdirir
    fmod : iki float degerinin modunu verir
    hypot: iki float degerinin hipotenunu verir
    isfinite ve isinf sonsuz deger doup donmedigini kontrol eder
    isgreater : bir degerin digerinden buyuk olup olmadigini kontrol eder
    isless ise kucuk olup olmadigini




    */
    return 0;
}
