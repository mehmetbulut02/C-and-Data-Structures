#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    char myKey;
    printf("Bir tusa basiniz:");
    myKey=getch();  // klavyeden tek bir karakter okumak icin kullanilir ( getchar da kullanilabilir tek fark enter a basmak)
    printf("%c tusuna bastiniz\n",myKey);
    printf("Tusun ASCII kodu:%d",myKey);
    return 0;
    */

    putchar(65); // Girilen sayinin ASCII karsiligini yazdirir
}
